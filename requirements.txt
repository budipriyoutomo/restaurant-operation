fastapi>=0.115.0
uvicorn[standard]>=0.30.6
gunicorn>=23.0.0
sqlalchemy>=2.0.35
alembic>=1.13.3
# psycopg3 (package name: psycopg) replaces psycopg2 — has pre-built wheels for Python 3.14
psycopg[binary]>=3.2.0
# pydantic 2.11+ uses pyo3 0.24+ which supports Python 3.14
pydantic>=2.11.0
pydantic-settings>=2.8.0
python-dotenv>=1.0.1

# Auth (JWT + bcrypt)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Rate limiting
slowapi>=0.1.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
